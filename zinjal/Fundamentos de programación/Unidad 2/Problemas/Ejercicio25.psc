Algoritmo Ejercicio25
	//a) 3 + 4 = 3 - 9 * 3 -> Resultado: FALSO
	//b) 145 < '789' -> No se pueden comparar cosas de distinto tipo. La primera es numérica y la segunda es una cadena de texto. 
	//c) FICH < 79 -> Si a fich se le asignó un valor numérico entonces se puden comparar.
	//d) TRUNC(x) <= REDON(x) -> Si X es un número se puede comparar.
	//e) no (54>97) -> Verdadero
	//f) ('a'<'c') ó (35>98) -> Verdadero porque a es menor que c. 
FinAlgoritmo
